# Privacy

Claude Desk 是本地桌面应用，不提供由本项目维护者运营的云端账户、同步或分析服务。

## 本机保存的数据

应用会在操作系统的 Application Support / App Data 目录保存：

- 项目和对话元数据；
- 本地 SQLite 对话记录；
- 用户主动添加的附件副本；
- 应用设置；
- 最近运行的有界诊断记录。

诊断设计只记录结构化计数、退出状态和经过截断及常见凭据脱敏的错误信息，不应保存完整聊天、stdout 或项目代码。用户可以在产品中导出诊断用于排查，导出文件由用户自行决定是否分享。

## 发送给模型的内容

Claude Desk 通过用户本机安装和配置的 Claude Code 运行请求。提示词、附件路径、项目内容、工具调用和模型响应可能由 Claude Code、用户选择的模型服务、MCP server 或其他用户配置的集成处理。其网络、保留和隐私行为由对应服务及用户配置决定，不由本项目控制。

## 项目文件

应用只在用户选择的项目范围内提供文件浏览、预览、Git 和本地产物操作。附件会复制到应用自己的数据目录。本地下载链接必须经过后端规范化和项目边界校验后才能打开。

## 网络边界

Claude Desk 自身没有维护者控制的遥测上报端点。应用仍会因以下行为产生网络访问：

- Claude Code 与其配置的模型服务通信；
- 用户配置的 MCP server、Skill 或工具访问网络；
- 用户主动打开网页或远程链接；
- 操作系统和依赖组件自身的更新或安全服务。

## 用户责任

不要在 Issue、Pull Request、诊断文件或截图中提交 API key、访问令牌、2FA 二维码、恢复码、内部代码或个人数据。公开分享前应先完成脱敏。
