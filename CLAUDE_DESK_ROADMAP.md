# Claude Desk 产品路线图

> 状态约定：`[ ]` 待评审 / `[~]` 开发中 / `[x]` 已完成并验收。
> 更新规则：每项开始前先确认范围；完成后补充版本、提交、验证证据和遗留风险，不把未完成能力表述为已支持。

## 产品边界

Claude Desk 是 Claude Code 的原生桌面产品，不是把 Claude CLI 或普通 Terminal 套进一个 WebView。

- 对话、工具活动、权限、文件改动、下载、服务运行和 Git 操作应呈现为产品交互；
- 允许“打开外部终端”作为便捷入口，但不在 App 内展示原始终端作为主界面；
- 长驻开发服务不能依赖一次 Claude 会话派生的后台子进程，应使用平台原生的独立进程机制管理。

## 当前已具备的基础

- [x] 本地项目、会话、草稿保留、会话排序与对话导航。
- [x] Claude 流式回答、活动时间线、停止、补充消息队列和“立即调整”。
- [x] 图片/文件附件、图片预览、项目内文件预览、受路径校验保护的下载卡片。
- [x] Changed Files / Diff 查看、代码块复制、权限、模型与连接设置。
- [x] 上下文显示与自动压缩入口、Skills 的 `/` 选择器。

## P0：下一阶段高频体验

### [ ] 1. `@文件`、`@目录` 与 `@Git diff` 上下文引用

**目标**：在输入框内输入 `@` 搜索当前项目文件、目录或当前 Git 改动，将选择结果作为结构化上下文发送，而不是依赖手动复制路径或上传文件。

**验收条件**

- [ ] 支持键盘搜索、选择、移除和重复引用；
- [ ] 引用内容在用户消息中清晰展示，回答中的文件引用可一键打开；
- [ ] 大目录、二进制、项目外路径和已删除文件有安全边界与提示；
- [ ] 不改变既有附件、中文输入法、补充队列行为。

### [ ] 2. 编辑消息、重新生成与分支对话

**目标**：允许用户修正历史提问或重新生成回复；保留原会话链路，并创建可命名、可切换的对话分支。

**验收条件**

- [ ] 编辑历史问题不会覆盖原消息或丢失附件；
- [ ] 重试、取消、失败恢复与正在运行状态行为一致；
- [ ] 分支可在侧栏识别、切换、删除和恢复；
- [ ] Claude 会话 ID、上下文统计、队列和文件改动不会串到错误分支。

## P1：让长期开发更顺手

### [~] 4. Git 工作流与代码审查升级

- [x] 环境信息面板：变更统计、项目路径、分支、上游同步状态、提交与推送确认。
- [x] 默认左右并排 Diff；窄窗口自动回退为上下对比。
- 按文件、状态、文本筛选改动；支持未跟踪文件和二进制提示；
- 提交前生成审查摘要、测试建议和风险项；
- 后续再引入“暂存选中、生成提交信息、提交、推送”，每一步保留确认与失败恢复。

### [ ] 5. 会话搜索、固定与归档

- 按标题、消息内容、文件路径筛选；
- 固定重要会话，归档不活跃会话并可恢复；
- 长列表采用分页/虚拟化，避免历史会话拖慢侧栏。

### [ ] 6. Skills 与 MCP 管理

- Skills：最近使用、收藏、说明/参数模板、作用域和不可用原因；
- MCP：项目级配置来源、启停状态、连通性测试、权限范围与错误诊断；
- 不在聊天中泄露密钥或原始配置值。

### [ ] 7. 任务通知与本轮交付摘要

- Claude 完成、失败、需要权限、服务掉线时发送本地通知；
- 每轮收束为“改动 / 验证 / 遗留事项”，可回看和跳转对应文件。

## P2：复杂项目与团队协作

### [ ] 8. Git Worktree 工作区

- 用独立工作目录承载并行功能、试验性重构与 review；
- 明确展示分支、目录、未提交改动和清理操作，避免误删。

### [ ] 9. 多根目录项目

- 一个 Claude Desk 项目可指定主目录和多个关联目录；
- 主目录决定 Claude、Git、设置与指令文件，关联目录用于检索、阅读和编辑。

### [ ] 10. 内置工作流命令

- 将 `/review`、`/test`、`/commit`、`/start` 做成带参数、权限与可验证结果的产品工作流；
- 与通用 Skills 区分：内置命令承担 App 可验证的本地能力，Skills 承担可复用的提示与流程知识。

### [ ] 11. 原生开发服务面板（可选增强）

- 当前 Claude Desk 的 AI 交互已经满足日常开发，不需要嵌入终端，也不需要为了对标 Codex 优先改造；
- 只有在明确需要由 App 长期托管本地服务时再实现，届时使用平台原生服务管理：macOS `launchd`、Windows 对应的独立进程或服务机制；
- 展示服务名称、端口、健康状态与日志入口，并确保 Claude 会话结束后服务仍可持续运行；
- Rust 层负责命令、路径与生命周期校验，前端只展示结构化状态。

## 暂不纳入路线图

- 内嵌 Claude CLI / 原始终端界面；
- 未经 Claude Code 运行时确认的多 Agent 调度；
- 未具备对应服务端能力时，直接复制 Codex 的云端委派功能。

## 参考

Codex 当前把代码审查、Skills/Plugins、Hooks、Git Worktree、环境和第三方集成作为开发工作流能力。Claude Desk 借鉴其交互目标，但优先选择符合本地 Claude Code 运行时与本产品边界的实现：<https://learn.chatgpt.com/docs/developers>。
